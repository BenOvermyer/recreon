"""AI system. Ports NPE.PAS, NPEINTR.PAS, NPETYPES.PAS and NPE00-NPE04.PAS.

Only ``types.py`` (NPETYPES.PAS) is ported so far; the AI behaviour modules
are Phase 7. See docs/ARCHITECTURE.md for the module map.
"""
